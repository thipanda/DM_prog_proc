/*
Fonction principale qui permet d'estimer le prix du logement x à partir des données du fichier csv
*/

#ifndef MAIN
#define MAIN

#include <stdlib.h>
#include <stdio.h>
#include <time.h>

#define TAB_SIZE 7917
#define MALLOC(x) ((x *) malloc(sizeof(x)))

#endif